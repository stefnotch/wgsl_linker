{
    "scripts": {
        "build": "wasm-pack build --dev --target=web --out-dir=./dist",
        "release": "wasm-pack build --release --target=web --out-dir=./dist && pnpm run size",
        "size": "node ./scripts/brotli-size.cjs"
    },
    "devDependencies": {
        "wasm-pack": "^0.14.0",
        "brotli-wasm": "^3.0.1"
    }
}