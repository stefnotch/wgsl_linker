[build]
rustflags = ["--cfg=has_std", "-Ctarget-feature=+multivalue"]

# https://github.com/johnthagen/min-sized-rust#abort-on-panic
[unstable]
build-std = ["std", "panic_abort", "core", "alloc"]
build-std-features = ["panic_immediate_abort"]
wasm-c-abi = ["spec"]
