{
    "scripts": {
        "build": "wasm-pack build --dev --target=web --out-dir=./dist",
        "release": "wasm-pack build --release --target=web --out-dir=./dist"
    },
    "devDependencies": {
        "wasm-pack": "^0.13.1"
    }
}