[workspace]
# Purposefully excluded, so that we can set a different .cargo/config.toml for it
exclude = ["linker_wasm"]
members = ["wesl_linker"]
resolver = "3"

[workspace.package]
license-file = "LICENSE"
readme = "README.md"
